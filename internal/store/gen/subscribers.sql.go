// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: subscribers.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const confirmSubscriber = `-- name: ConfirmSubscriber :one
UPDATE mailing_list_subscribers
SET status = 'confirmed',
    confirmed_at = now(),
    confirm_token_hash = NULL,
    confirm_expires_at = NULL,
    updated_at = now()
WHERE confirm_token_hash = $1
  AND status = 'pending'
  AND (confirm_expires_at IS NULL OR confirm_expires_at > now())
RETURNING id, email, name, status, confirmed_at
`

type ConfirmSubscriberRow struct {
	ID          uuid.UUID
	Email       string
	Name        pgtype.Text
	Status      SubscriberStatus
	ConfirmedAt pgtype.Timestamptz
}

// Complete double opt-in: match the (unexpired) confirm token, flip to confirmed,
// and clear the token so the link cannot be replayed.
func (q *Queries) ConfirmSubscriber(ctx context.Context, confirmTokenHash []byte) (ConfirmSubscriberRow, error) {
	row := q.db.QueryRow(ctx, confirmSubscriber, confirmTokenHash)
	var i ConfirmSubscriberRow
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.Status,
		&i.ConfirmedAt,
	)
	return i, err
}

const countSubscribersByStatus = `-- name: CountSubscribersByStatus :many
SELECT status, count(*) AS total
FROM mailing_list_subscribers
GROUP BY status
`

type CountSubscribersByStatusRow struct {
	Status SubscriberStatus
	Total  int64
}

func (q *Queries) CountSubscribersByStatus(ctx context.Context) ([]CountSubscribersByStatusRow, error) {
	rows, err := q.db.Query(ctx, countSubscribersByStatus)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CountSubscribersByStatusRow{}
	for rows.Next() {
		var i CountSubscribersByStatusRow
		if err := rows.Scan(&i.Status, &i.Total); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getSubscriberByEmail = `-- name: GetSubscriberByEmail :one
SELECT id, email, name, status, confirm_token_hash, confirm_expires_at,
       unsubscribe_token_hash, source, confirmed_at, unsubscribed_at, created_at, updated_at
FROM mailing_list_subscribers
WHERE email = $1
`

func (q *Queries) GetSubscriberByEmail(ctx context.Context, email string) (MailingListSubscriber, error) {
	row := q.db.QueryRow(ctx, getSubscriberByEmail, email)
	var i MailingListSubscriber
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.Status,
		&i.ConfirmTokenHash,
		&i.ConfirmExpiresAt,
		&i.UnsubscribeTokenHash,
		&i.Source,
		&i.ConfirmedAt,
		&i.UnsubscribedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listConfirmedSubscribers = `-- name: ListConfirmedSubscribers :many
SELECT id, email, name, source, confirmed_at, created_at
FROM mailing_list_subscribers
WHERE status = 'confirmed'
  AND (created_at, id) > ($1, $2)
ORDER BY created_at, id
LIMIT $3
`

type ListConfirmedSubscribersParams struct {
	AfterCreatedAt pgtype.Timestamptz
	AfterID        pgtype.Timestamptz
	RowLimit       int32
}

type ListConfirmedSubscribersRow struct {
	ID          uuid.UUID
	Email       string
	Name        pgtype.Text
	Source      pgtype.Text
	ConfirmedAt pgtype.Timestamptz
	CreatedAt   pgtype.Timestamptz
}

// ADMIN ONLY. Keyset pagination over (created_at, id) for CSV export.
func (q *Queries) ListConfirmedSubscribers(ctx context.Context, arg ListConfirmedSubscribersParams) ([]ListConfirmedSubscribersRow, error) {
	rows, err := q.db.Query(ctx, listConfirmedSubscribers, arg.AfterCreatedAt, arg.AfterID, arg.RowLimit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListConfirmedSubscribersRow{}
	for rows.Next() {
		var i ListConfirmedSubscribersRow
		if err := rows.Scan(
			&i.ID,
			&i.Email,
			&i.Name,
			&i.Source,
			&i.ConfirmedAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const unsubscribeByToken = `-- name: UnsubscribeByToken :one
UPDATE mailing_list_subscribers
SET status = 'unsubscribed',
    unsubscribed_at = now(),
    updated_at = now()
WHERE unsubscribe_token_hash = $1 AND status <> 'unsubscribed'
RETURNING id, email, status
`

type UnsubscribeByTokenRow struct {
	ID     uuid.UUID
	Email  string
	Status SubscriberStatus
}

func (q *Queries) UnsubscribeByToken(ctx context.Context, unsubscribeTokenHash []byte) (UnsubscribeByTokenRow, error) {
	row := q.db.QueryRow(ctx, unsubscribeByToken, unsubscribeTokenHash)
	var i UnsubscribeByTokenRow
	err := row.Scan(&i.ID, &i.Email, &i.Status)
	return i, err
}

const upsertSubscriber = `-- name: UpsertSubscriber :one

INSERT INTO mailing_list_subscribers
    (id, email, name, status, confirm_token_hash, confirm_expires_at, unsubscribe_token_hash, source)
VALUES ($1, $2, $3, 'pending', $4, $5, $6, $7)
ON CONFLICT (email) DO UPDATE SET
    name = COALESCE(EXCLUDED.name, mailing_list_subscribers.name),
    status = CASE WHEN mailing_list_subscribers.status = 'confirmed'
                  THEN 'confirmed'::subscriber_status
                  ELSE 'pending'::subscriber_status END,
    confirm_token_hash = CASE WHEN mailing_list_subscribers.status = 'confirmed'
                              THEN mailing_list_subscribers.confirm_token_hash
                              ELSE EXCLUDED.confirm_token_hash END,
    confirm_expires_at = CASE WHEN mailing_list_subscribers.status = 'confirmed'
                              THEN mailing_list_subscribers.confirm_expires_at
                              ELSE EXCLUDED.confirm_expires_at END,
    updated_at = now()
RETURNING id, email, name, status, confirm_token_hash, confirm_expires_at,
          unsubscribe_token_hash, source, confirmed_at, unsubscribed_at, created_at, updated_at
`

type UpsertSubscriberParams struct {
	ID                   uuid.UUID
	Email                string
	Name                 pgtype.Text
	ConfirmTokenHash     []byte
	ConfirmExpiresAt     pgtype.Timestamptz
	UnsubscribeTokenHash []byte
	Source               pgtype.Text
}

// Mailing-list subscribers (double opt-in). Raw tokens live only in the emailed
// links; these queries deal exclusively in their sha256 hashes.
// Idempotent signup. A brand-new address is inserted as 'pending'. A previously
// unsubscribed (or still-pending) address is reset to 'pending' with a fresh
// confirmation token, so someone can always re-subscribe. An already-confirmed
// address is left untouched (its status stays 'confirmed').
func (q *Queries) UpsertSubscriber(ctx context.Context, arg UpsertSubscriberParams) (MailingListSubscriber, error) {
	row := q.db.QueryRow(ctx, upsertSubscriber,
		arg.ID,
		arg.Email,
		arg.Name,
		arg.ConfirmTokenHash,
		arg.ConfirmExpiresAt,
		arg.UnsubscribeTokenHash,
		arg.Source,
	)
	var i MailingListSubscriber
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.Name,
		&i.Status,
		&i.ConfirmTokenHash,
		&i.ConfirmExpiresAt,
		&i.UnsubscribeTokenHash,
		&i.Source,
		&i.ConfirmedAt,
		&i.UnsubscribedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
